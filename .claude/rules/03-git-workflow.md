# Git Workflow

- Small, regular, descriptive commits.
- Kanban-style: commit each complete change with passing tests before moving on.
- Do not batch unrelated work.
- Every agent commit must include: `Co-authored-by: Claude`
- Do not make speculative architectural changes without explicit user confirmation.
