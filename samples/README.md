# Samples

This directory contains intentionally bad Ruby files used as sample input for
`cleo_quality_review`.

The files are expected to trigger findings from the included quality tools, so
they should not be treated as examples of production code.
